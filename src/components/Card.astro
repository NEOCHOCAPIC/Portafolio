---
const { title, description, imageUrl, altText, link } = Astro.props;
---

<div
  class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden transition duration-300 transform hover:scale-105"
>
  <img src={imageUrl} alt={altText} class="w-full h-48 object-cover" />
  <div class="p-4">
    <h3 class="text-xl font-semibold mb-2">{title}</h3>
    <p class="text-gray-600 dark:text-gray-400 mb-4">{description}</p>
    <a
      href={link}
      target="_blank"
      class="text-teal-500 hover:text-teal-600 dark:text-teal-400 dark:hover:text-teal-300"
      >Ver más →</a
    >
  </div>
</div>
